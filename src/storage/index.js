// storage分裝
const STORAGE_KEY ='mail' ;

export default{

    getItem(){
        if (model_name){
            let val = this.getStorge(model_name)
        }
    this.getStorge()[key];
    },
    getStorge(){
    return JSON.parse(window.sessionStorage.getStorge)
    },
    clean(){

    }
}